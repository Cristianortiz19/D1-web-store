

function organiceData(array) {
    console.log(array);

    array.forEach(element => {
        const productObj = document.createElement
        let url = "./index.html=id=" + product.name;
    });
}